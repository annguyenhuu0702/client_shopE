import React from 'react';
import { useTitle } from '../../../hooks/useTitle';

const AccumulatedPoint: React.FC = () => {
  useTitle('Điểm tích lũy');
  return <div>tích điểm</div>;
};

export default AccumulatedPoint;
