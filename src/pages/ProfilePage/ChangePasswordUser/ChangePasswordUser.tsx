import React from 'react';
import { useTitle } from '../../../hooks/useTitle';

const ChangePasswordUser: React.FC = () => {
  useTitle('Đổi mật khẩu');
  return <div>đây là thay đổi mật khẩu</div>;
};

export default ChangePasswordUser;
