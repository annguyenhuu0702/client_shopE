import React from 'react';
import { useTitle } from '../../hooks/useTitle';

const ProfilePage: React.FC = () => {
  useTitle('Tài khoản của tôi');
  return <div className="max-sm:mt-24">aaa</div>;
};

export default ProfilePage;
